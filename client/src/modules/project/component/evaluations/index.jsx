import React from 'react'
import { connect } from 'react-redux'

const index = (props) => {
    return (
        <div>
            Đây là Trang đánh giá
        </div>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(index)
